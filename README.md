Guide to using:

first, import it like any other file (import it all and not only the function)

import printColored as colors # shorten it so u dont have to type printColored.printColored() everytime

now, to use the function,

colors.printColored("endline", "text", **"kwargs for formats")

you can use it like:

colors.printColored(" ", "Hello", "red", "underline")

colors.printColored("", "World", "blue", "double_underline")

print("Test")

wait 1 sec and i continue the readme
