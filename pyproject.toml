[project]
name = "printcolored"
version = "2.1.1"
dependencies = ["platformdirs"]

[project.optional-dependencies]
windows-legacy = ["colorama"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"